<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <IsTestProject>true</IsTestProject>
    <IsPackable>false</IsPackable>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- The test adapter is required for dotnet test discovery; the application
         itself keeps its existing dependency set. -->
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\ProjectCostForecast.App\ProjectCostForecast.App.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Update="Fixtures\ProjectFiles\*.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <ItemGroup>
    <!-- MainWindow uses application-relative image URIs. The WPF test host
         owns ResourceAssembly, so link the app resources into that assembly
         for the representative window smoke path. -->
    <Resource Include="..\..\src\ProjectCostForecast.App\Assets\Icons\png\*.png"
              Link="Assets\Icons\png\%(Filename)%(Extension)" />
    <Resource Include="..\..\src\ProjectCostForecast.App\Assets\Backgrounds\*.png"
              Link="Assets\Backgrounds\%(Filename)%(Extension)" />
  </ItemGroup>
</Project>
