# Luna packet ledger

This ledger records one packet at a time. Existing work before the checkpoint is treated as baseline evidence, not as audit implementation.

## LUNA-00

Packet: LUNA-00 - Baseline and recoverable checkpoint
Status: complete
Baseline commit: `4f1fc24` (`chore(audit): checkpoint pre-audit working tree`)
Changed files: `docs/audit/BASELINE.md`, `docs/audit/PACKET_LEDGER.md`, plus the pre-existing worktree preserved by the checkpoint commit.
Characterization evidence: baseline restore, Release build, console acceptance, and diff-check commands recorded in `docs/audit/BASELINE.md`.
Commands and results: `git add -A`, checkpoint commit, and `git push origin alpha/1.13-1.22` all succeeded.
Manual checks: confirmed the branch and mixed worktree state before editing.
Residual risks / follow-up packet: the baseline commit includes broad pre-existing changes and is not an audit-quality feature diff.

## LUNA-01

Packet: LUNA-01 - One-command local verification
Status: complete
Baseline commit: `4f1fc24`
Changed files: `scripts/verify.ps1`, `README.md`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: the script invokes restore, Release build, and the console harness through one repository-rooted PowerShell entry point; its command runner throws on any non-zero native exit code.
Commands and results: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1'` exited 0 with restore, Release build, and the full console harness passing. The same command with `-NoRestore` exited 0. Invoking the script by its absolute path from `C:\Windows\Temp` with `-NoRestore` exited 0. Invoking it with `-NoRestore -DotnetCommand where.exe` made the child exit 2 and the script exit 1.
Manual checks: the repository path containing spaces was exercised; generated build output remained under ignored `bin/` and `obj/` directories; the failure path reported the child exit code.
Residual risks / follow-up packet: the existing console harness remains an executable rather than a discovered test project; that is LUNA-02 scope.

## LUNA-02

Packet: LUNA-02 - Discovered-test foundation
Status: complete
Baseline commit: `4f1fc24`
Changed files: `tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj`, `tests/ProjectCostForecast.UnitTests/PersistenceAndCalculationTests.cs`, `ProjectCostForecast.sln`, `scripts/verify.ps1`, `README.md`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: four discovered tests cover calculation totals, nested atomic project save/load, replacement of an existing project, and rapid distinct loadable backups. Each test owns a unique temporary directory and deletes it during disposal; no application/UI process is started. The legacy console harness remains a separate mandatory gate.
Commands and results: `dotnet restore ProjectCostForecast.sln` exited 0; `dotnet build ProjectCostForecast.sln -c Release --no-restore` exited 0 with 0 warnings and 0 errors; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore` discovered and passed 4 tests; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore` exited 0 with both test gates passing; `git diff --check` exited 0.
Manual checks: the verifier was exercised from the repository path containing spaces. Test-only `Microsoft.NET.Test.Sdk` 17.11.1, `xunit` 2.9.2, and `xunit.runner.visualstudio` 2.8.2 were added because conventional discovery requires a test SDK/framework/adapter; no production dependency was added. No STA mechanism was added because the ported tests are headless service/persistence tests and create no WPF windows.
Residual risks / follow-up packet: WPF-specific tests still need a narrowly scoped STA fixture if a later packet introduces them; broader characterization remains in the console harness until incrementally ported.

## LUNA-03

Packet: LUNA-03 - Safe close with unsaved changes
Status: complete
Baseline commit: `4f1fc24`
Changed files: `src/ProjectCostForecast.App/Services/CloseDecisionPolicy.cs`, `src/ProjectCostForecast.App/MainWindow.xaml.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ProjectIO.cs`, `src/ProjectCostForecast.App/ReimplementationTodoWindow.cs`, `docs/RELEASE_NOTES.md`, `tests/ProjectCostForecast.UnitTests/SafeCloseTests.cs`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: clean close bypasses prompting; dirty close requires an explicit Save, Discard, or Cancel decision; Cancel preserves dirty state; Discard clears the pending dirty state without saving; Save clears dirty state only after persistence succeeds; and a forced persistence exception leaves the close rejected and dirty state intact. The close handler also guards against re-entrant close events.
Commands and results: `dotnet build ProjectCostForecast.sln -c Release --no-restore` exited 0 with 0 warnings and 0 errors; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore` exited 0 with 9 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore` exited 0 with the legacy harness and 9 discovered tests passing; `git diff --check` exited 0.
Manual checks: the window close path now maps Yes / No / Cancel to Save / Discard / Cancel, persists preferences only after close approval, and retains the existing Save command error dialog while avoiding a modal error dialog during headless close-policy tests. The TODO window and release notes now describe the implemented behavior.
Residual risks / follow-up packet: Save As remains a WPF file-picker path and is not automated in the headless suite; dialog and broader open/save boundary extraction remain LUNA-15A scope. Transactional New Month remains the next P0 packet, LUNA-04.

## LUNA-04

Packet: LUNA-04 - Transactional and idempotent New Month
Status: complete
Baseline commit: `2ff4158`
Changed files: `src/ProjectCostForecast.App/Services/ProjectDatasetCloner.cs`, `src/ProjectCostForecast.App/Services/NewMonthOperation.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModelDependencies.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ProjectIO.cs`, `tests/ProjectCostForecast.UnitTests/NewMonthTests.cs`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: New Month now prepares a serialized clone, recalculates and adds the single baseline/audit set on the staged dataset, persists it, and reloads it into live state only after success. Cancelled and failed saves leave period, forecast values, snapshots, audit IDs, selected objects, and dirty state unchanged. A duplicate current-period baseline is rejected, and a re-entrant invocation during persistence is rejected by an interlocked guard.
Commands and results: `dotnet build ProjectCostForecast.sln -c Release --no-restore` exited 0 with 0 warnings and 0 errors; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore` exited 0 with 14 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore` exited 0 with the legacy harness and 14 discovered tests passing; `git diff --check` exited 0.
Manual checks: successful rollover advances `26-09` to `26-10`, creates one `26-09` snapshot, rolls prior-month values once, preserves the selected forecast line by identity key after commit, and clears dirty state only after staged persistence succeeds. The normal Save command still persists the live dataset and retains its existing Save As behavior.
Residual risks / follow-up packet: Save As remains a WPF file-picker path and is not automated in the headless suite; project format versioning and migration remain LUNA-05 scope.

## LUNA-05

Packet: LUNA-05 - Versioned project format and migration pipeline
Status: complete
Baseline commit: `5f6d508`
Changed files: `src/ProjectCostForecast.App/Models/ProjectDataset.cs`, `src/ProjectCostForecast.App/Services/ProjectDatasetMigrationPipeline.cs`, `src/ProjectCostForecast.App/Services/ProjectFileService.cs`, `src/ProjectCostForecast.App/Services/SampleDataService.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModelDependencies.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Dataset.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Presentation.cs`, `tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj`, `tests/ProjectCostForecast.UnitTests/PersistenceAndCalculationTests.cs`, `tests/ProjectCostForecast.UnitTests/Fixtures/ProjectFiles/*.json`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: Project files now carry an explicit format version independent of the app version. Unversioned format 0 files are normalized and migrated to format 1; root and nested null lists, resource-comment defaults, period labels, canonical dates, and missing continuous periods are handled by the WPF-free pipeline. Top-level JSON null, malformed content, invalid version values, and unsupported future versions produce typed, actionable format errors before deserialization can replace the active session.
Commands and results: `dotnet build ProjectCostForecast.sln -c Release --no-restore` exited 0 with 0 warnings and 0 errors; focused persistence/migration tests exited 0 with 10 tests passed; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore` exited 0 with 20 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore` exited 0 with the legacy harness and 20 discovered tests passing; `git diff --check` exited 0.
Manual checks: legacy, current v1, null-root, malformed, and future-v99 fixtures were loaded through `ProjectFileService`; legacy output saved with `FormatVersion` 1; a second normalization reported no data changes; current fixture values and audit identity round-tripped unchanged. The existing open-project path loads and migrates before `LoadDataset`, so format errors are caught without replacing the active dataset.
Residual risks / follow-up packet: required-field, duplicate-identity, numeric-bound, and post-migration validation remain LUNA-06A; stale-write protection remains LUNA-06B; backup verification and restore remain LUNA-07.

## LUNA-06A

Packet: LUNA-06A - Validation at operation boundaries
Status: complete
Baseline commit: `76a5805` (`feat(audit): complete LUNA-05 project format migration`)
Changed files: `src/ProjectCostForecast.App/MainWindow.xaml`, `src/ProjectCostForecast.App/Models/AuditEvent.cs`, `src/ProjectCostForecast.App/Services/ProjectFileService.cs`, `src/ProjectCostForecast.App/Services/ValidationContracts.cs`, `src/ProjectCostForecast.App/Services/ValidationService.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Dataset.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ProjectIO.cs`, `tests/ProjectCostForecast.UnitTests/ValidationBoundaryTests.cs`, `tests/ProjectCostForecast.UnitTests/TestAssemblySettings.cs`, `tests/ProjectCostForecast.UnitTests/NewMonthTests.cs`, `tests/ProjectCostForecast.UnitTests/PersistenceAndCalculationTests.cs`, `tests/ProjectCostForecast.UnitTests/Fixtures/ProjectFiles/legacy-unversioned.json`, and `tests/ProjectCostForecast.Tests/Program.cs`.
Characterization evidence: validation issues now carry stable codes and explicit `Error` / `Warning` severities. Required project, forecast-line, transaction, period, duplicate-identity, date-alignment, and financial-bound rules are reported with actionable remedies. Errors block project save before backup/audit/atomic write, block New Month before staging and again before persistence, and block imports against a cloned candidate before any transaction or forecast-line commit; warning-only reports remain saveable and visible. Project-file load validates the migrated dataset, and the open-project path validates the normalized candidate before replacing the active session.
Commands and results: `dotnet build ProjectCostForecast.sln -c Release --no-restore` exited 0 with 0 warnings and 0 errors; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj --no-restore --filter "FullyQualifiedName~ValidationBoundaryTests"` exited 0 with 8 focused tests passed; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore` exited 0 with 28 discovered tests passed; `dotnet run --project tests/ProjectCostForecast.Tests/ProjectCostForecast.Tests.csproj -c Release --no-build` exited 0 with the full legacy harness passing; `git diff --check` passed.
Manual checks: invalid save state leaves dirty state set and does not call the file service; invalid New Month leaves the current period, forecast values, snapshots, audit history, and save count unchanged; invalid CSV import leaves transactions, forecast lines, audit history, and dirty state unchanged; invalid project files raise a typed validation exception with the operation and remedy; the validation grid now exposes the stable issue code.
Residual risks / follow-up packet: Save As and broader WPF dialog extraction remain LUNA-15A scope; same-file stale-write protection is covered by LUNA-06B; backup verification and restore remain LUNA-07. Existing user-cancelled import preview behaviour still routes unresolved combinations to the unmatched list by design.

## LUNA-06B

Packet: LUNA-06B - Same-file stale-write protection
Status: complete
Baseline commit: `7e93dee` (`feat(audit): complete LUNA-06A validation boundaries`)
Changed files: `src/ProjectCostForecast.App/Services/ProjectFileRevision.cs`, `src/ProjectCostForecast.App/Services/ProjectFileService.cs`, `src/ProjectCostForecast.App/Services/ServiceContracts.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModelDependencies.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ProjectIO.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ViewsAndFilters.cs`, `tests/ProjectCostForecast.UnitTests/StaleWriteTests.cs`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: project loads return a content-hash revision token; revision-aware saves compare the expected token immediately before atomic replacement and throw a typed conflict without overwriting a newer file. Successful saves return the new token for the next single-instance save. The view model exposes a headless decision seam and maps Reload to loading the newer file, Save As to the existing Save As flow, and Cancel to retaining dirty state. The conflict prompt explicitly states that field-level merge is unsupported.
Commands and results: `dotnet build ProjectCostForecast.sln -c Release --no-restore` exited 0 with 0 warnings and 0 errors; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~StaleWriteTests"` exited 0 with 3 tests passed; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore` exited 0 with 31 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore` exited 0 with the legacy harness and 31 discovered tests passing; `git diff --check` passed.
Manual checks: two independent `ProjectFileService` instances reproduce a newer external save followed by a stale save attempt; the stale attempt raises `ProjectFileConflictException`, reports Reload / Save As / Cancel guidance, and leaves the newer title on disk. A view-model test injects Cancel without opening a WPF dialog and confirms dirty state and the external file remain unchanged. Existing non-revisioned `Save` calls retain overwrite behavior for explicit Save As and legacy callers.
Residual risks / follow-up packet: this is whole-file conflict detection, not field-level merge; users must Reload or Save As when the token changes. Backup integrity, retention, and restore are covered by LUNA-07; broader dialog extraction remains LUNA-15A.

## LUNA-07

Packet: LUNA-07 - Backup integrity, retention, and restore drill
Status: complete
Baseline commit: `df493c4` (`feat(audit): complete LUNA-06B stale-write protection`)
Changed files: `README.md`, `docs/ARCHITECTURE.md`, `docs/RELEASE_NOTES.md`, `docs/RECOVERY_RUNBOOK.md`, `src/ProjectCostForecast.App/MainWindow.xaml`, `src/ProjectCostForecast.App/Services/ProjectBackupContracts.cs`, `src/ProjectCostForecast.App/Services/ProjectFileService.cs`, `src/ProjectCostForecast.App/Services/ServiceContracts.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ProjectIO.cs`, `tests/ProjectCostForecast.UnitTests/BackupRecoveryTests.cs`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: backups are copied with deterministic timestamp/suffix collision handling, parsed through migration, and validated before they are reported as usable. A default policy retains the 10 newest backup names and never permits a configured policy below two. Retention runs only after verification and preserves a usable backup when older candidates are pruned. Restore verifies the source before any destination write, defaults to a new path, and creates a pre-restore backup before replacing an existing destination. The WPF project actions expose the recovery workflow; corrupt backups leave the current project and destination unchanged.
Commands and results: `dotnet build ProjectCostForecast.sln -c Release --no-restore` exited 0 with 0 warnings and 0 errors; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~BackupRecoveryTests"` exited 0 with 4 tests passed; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore` exited 0 with 35 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore` exited 0 with the legacy harness and 35 discovered tests passing; `git diff --check` passed.
Manual checks: a fixed clock produces distinct collision suffixes, the retention policy leaves exactly two valid backups in the bounded test, new-path restore round-trips project data, overwrite restore preserves the prior destination in a verified pre-restore backup, and a malformed backup is rejected before the current project changes. `docs/RECOVERY_RUNBOOK.md` gives non-source-code recovery steps and recommends separate-drive or approved cloud copies because same-directory backups do not cover directory loss.
Residual risks / follow-up packet: backups remain whole-file snapshots rather than field-level merge points; same-directory copies require off-device replication for disaster recovery. Runtime diagnostics and corrupt-preference handling remain LUNA-08; broader dialog extraction remains LUNA-15A.

## LUNA-08

Packet: LUNA-08 - Runtime diagnostics and corrupt preferences
Status: complete
Baseline commit: `e3ce252` (`feat(audit): complete LUNA-07 backup recovery`)
Changed files: `README.md`, `docs/ARCHITECTURE.md`, `docs/DIAGNOSTICS_RUNBOOK.md`, `docs/RELEASE_NOTES.md`, `src/ProjectCostForecast.App/App.xaml.cs`, `src/ProjectCostForecast.App/Services/DiagnosticsService.cs`, `src/ProjectCostForecast.App/Services/ServiceContracts.cs`, `src/ProjectCostForecast.App/Services/UserPreferencesService.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `tests/ProjectCostForecast.UnitTests/DiagnosticsTests.cs`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: `DiagnosticsService` writes operation, exception type, severity, and sanitized reason to a bounded active log plus one rotated copy by default. It excludes exception messages and redacts paths, quoted values, and named values from supplied reasons; any diagnostics write failure is swallowed. `UserPreferencesService` now quarantines malformed preference JSON with a UTC timestamp and collision suffix, preserves the bad content, loads defaults, records the typed failure, and exposes a generic startup notice through the view model. The WPF composition root logs dispatcher, application-domain, and unobserved-task failures; dispatcher/application-domain failures follow fail-fast handling, while isolated unobserved tasks are marked observed.
Commands and results: `dotnet build ProjectCostForecast.sln -c Release --no-restore` exited 0 with 0 warnings and 0 errors; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore --filter "FullyQualifiedName~DiagnosticsTests"` exited 0 with 6 focused tests passed; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore` exited 0 with 41 tests passed; `dotnet run --project tests/ProjectCostForecast.Tests/ProjectCostForecast.Tests.csproj -c Release --no-build` exited 0 with the legacy harness passing; `git diff --check` exited 0.
Manual checks: the malformed-preference test preserves the exact prior JSON under `user-preferences.corrupt-YYYYMMDD-HHMMSS-fff.json`, confirms defaults and the startup notice, and confirms the log contains `JsonException` without the private project value or preference path. The bounded-log test retains no more than two files and 512 bytes with its reduced test policy. Runtime-policy tests confirm a UI boundary requests fail-fast, an unobserved task continues, and a throwing diagnostics sink cannot escape its boundary.
Residual risks / follow-up packet: existing feature-level catches and performance `Debug.WriteLine` calls remain local diagnostics debt; LUNA-08 centralizes only preferences and top-level runtime boundaries. Broader WPF dialog extraction remains LUNA-15A.

## LUNA-09

Packet: LUNA-09 - CSV and workbook boundary hardening
Status: complete
Baseline commit: `e6749bb` (`feat(audit): complete LUNA-08 diagnostics`)
Changed files: `README.md`, `docs/ARCHITECTURE.md`, `docs/RELEASE_NOTES.md`, `src/ProjectCostForecast.App/Services/CsvTransactionService.cs`, `src/ProjectCostForecast.App/Services/ImportBoundaryContracts.cs`, `src/ProjectCostForecast.App/Services/WorkbookBoundaryPreflight.cs`, `tests/ProjectCostForecast.UnitTests/ImportBoundaryTests.cs`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: spreadsheet formula prefixes in exported text are neutralized after ignorable leading characters at the CSV boundary, while numeric fields and canonical model/JSON values remain unchanged. CSV parsing is strict and streaming. XLSX/XLSM ZIP/XML preflight bounds compressed and expanded size, worksheets, rows, and cells before materialization; the workbook adapter then enforces column and cell-text limits before a complete batch is exposed. Typed failures provide safe actionable messages, cancellation and malformed inputs expose no partial rows, and all file/workbook handles close. Re-import follows the documented duplicate-key policy: matching transactions are skipped, and an all-duplicate batch leaves state and dirty tracking unchanged.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~ImportBoundaryTests"` exited 0 with 12 focused tests passed; the integrated packet filter exited 0 with 45 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 86 discovered tests, and all 428 retained smoke assertions passing; `git diff --check` passed.
Manual checks: formula fixtures beginning with `=`, `+`, `-`, and `@` remain unchanged in the source object but are inert in exported CSV; a negative decimal remains numeric. Multiline quoted CSV fields round-trip, malformed CSV and workbook containers return typed failures, reduced test limits reject oversized files/worksheets/rows/cells, cancellation returns no batch, supported `.xlsx` and `.xlsm` inputs import successfully, and imported files can be moved immediately afterward.
Residual risks / follow-up packet: the limits bound desktop resource use but remain policy defaults rather than per-project configuration. Import/export dialog extraction remains LUNA-15B; ClosedXML patching and dependency audit remain LUNA-21.

## LUNA-10

Packet: LUNA-10 - Date, time-zone, and locale contract
Status: complete
Baseline commit: `bd0fd00` (`feat(audit): complete LUNA-09 import boundaries`)
Changed files: `README.md`, `docs/ARCHITECTURE.md`, `docs/DATE_TIME_CONTRACT.md`, `docs/RELEASE_NOTES.md`, `src/ProjectCostForecast.App/ForecastCurvePresetWindows.cs`, `src/ProjectCostForecast.App/MainWindow.Gantt.cs`, `src/ProjectCostForecast.App/MainWindow.GridFilters.cs`, `src/ProjectCostForecast.App/MainWindow.ScheduleCommands.cs`, `src/ProjectCostForecast.App/MainWindow.SpreadsheetGridInteraction.cs`, `src/ProjectCostForecast.App/MainWindow.WorkspacePanels.cs`, `src/ProjectCostForecast.App/MainWindow.xaml`, `src/ProjectCostForecast.App/Models/AppUserPreferences.cs`, `src/ProjectCostForecast.App/Models/AuditEvent.cs`, `src/ProjectCostForecast.App/Models/ForecastLine.cs`, `src/ProjectCostForecast.App/Models/ProjectDataset.cs`, `src/ProjectCostForecast.App/Models/Schedule.cs`, `src/ProjectCostForecast.App/MonthlyReportCanvasInteractions.cs`, `src/ProjectCostForecast.App/MonthlyReportCharts.cs`, `src/ProjectCostForecast.App/SavedMonthSnapshotWindow.cs`, `src/ProjectCostForecast.App/Services/DateTimeContract.cs`, `src/ProjectCostForecast.App/Services/DiagnosticsService.cs`, `src/ProjectCostForecast.App/Services/InitialCostLoadService.cs`, `src/ProjectCostForecast.App/Services/NewMonthOperation.cs`, `src/ProjectCostForecast.App/Services/ProjectDatasetCloner.cs`, `src/ProjectCostForecast.App/Services/ProjectDatasetMigrationPipeline.cs`, `src/ProjectCostForecast.App/Services/ProjectFileService.cs`, `src/ProjectCostForecast.App/Services/SampleDataService.cs`, `src/ProjectCostForecast.App/Services/SchedulingService.cs`, `src/ProjectCostForecast.App/Services/UserPreferencesService.cs`, `src/ProjectCostForecast.App/UnmatchedImportWindow.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.CurvePresets.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ForecastColumns.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.MonthlyReport.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Presentation.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ProjectIO.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Schedule.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.TotalsAndPreferences.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ViewsAndFilters.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModelDependencies.cs`, `tests/ProjectCostForecast.UnitTests/DateTimeLocaleContractTests.cs`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: the contract separates NZ business dates (`DateOnly`), NZ-local presentation, and durable UTC `DateTimeOffset` instants. `IClock` supplies workflow instants and the NZ business date; audit events, saved-month snapshots, comments, mappings, unmatched-import records, baselines, curve presets, diagnostics, backups, and preference quarantine use the seam. Persistence converters normalize explicit offsets to UTC and interpret offset-free legacy timestamps as `Pacific/Auckland` local time. Ambiguous DST fall-back values use the standard offset, spring-forward gaps fail explicitly, invariant UTC strings/file names are culture-independent, and en-NZ display conversion does not alter the stored instant.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore --filter "FullyQualifiedName~DateTimeLocaleContractTests"` exited 0 with 7 focused tests passed; the integrated packet filter exited 0 with 45 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 86 discovered tests, and all 428 retained smoke assertions passing; the ambient-time audit found only `SystemClock` as a durable source, plus three intentionally retained `DateTime.UtcNow` reads for a transient 650 ms UI hover debounce; `git diff --check` passed.
Manual checks: fixed instants across both 2026 NZ DST boundaries preserve their UTC instant and expected local offset/date; the UTC-to-NZ year rollover produces 1 January 2027; offset-free legacy project and preference JSON normalizes to UTC while keeping fiscal labels/month starts; save/reopen under `en-US` writes invariant UTC and displays en-NZ; injected New Month time stamps the snapshot and both audit events exactly.
Residual risks / follow-up packet: this timestamp representation change necessarily propagates through many model owners and UI bindings, exceeding the normal production-file cap; policy and conversion logic remain centralized in `DateTimeContract`, while the remaining edits are typed persistence/display propagation. Existing project format version 1 remains readable in both directions because legacy and explicit-offset timestamp forms are accepted. Broader state ownership and model separation remain LUNA-12 through LUNA-14.

## LUNA-11

Packet: LUNA-11 - Complete discovered-test migration
Status: complete
Baseline commit: `96156f6` (`feat(audit): complete LUNA-10 time contract`)
Changed files: `README.md`, `docs/ARCHITECTURE.md`, `docs/RELEASE_NOTES.md`, `docs/TEST_COVERAGE_MAP.md`, `scripts/verify.ps1`, `tests/ProjectCostForecast.UnitTests/Luna11CalculationCoverageTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna11CurveCoverageTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna11GlobalUsings.cs`, `tests/ProjectCostForecast.UnitTests/Luna11ImportCoverageTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna11NegativeControlTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna11SchedulingCoverageTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna11TestSupport.cs`, `tests/ProjectCostForecast.UnitTests/Luna11ViewModelCoverageTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna11WorkspaceCoverageTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna11WpfGridCoverageTests.cs`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: the map accounts for all 428 logical assertion invocations across the console harness's 24 sections and points each section to named xUnit coverage. Twenty-six discovered tests port calculation, persistence, import, workspace, curve, scheduling, view-model, performance, and shared-grid checks. WPF behavior runs on a dedicated STA thread under a hidden `HwndSource`; every test creates fresh state and temporary paths. A negative control verifies a representative false equality raises xUnit's discovered `EqualException`. The verifier now has one default automated-test path—build then discovered tests—while the unchanged console executable has the distinct opt-in `-RunLegacySmoke` role.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~Luna11"` exited 0 with 26 focused tests passed; the integrated packet filter exited 0 with 45 tests passed; the full discovered suite exited 0 with 86 tests passed. A temporary representative failing `[Fact]` made the filtered `dotnet test` command exit 1 and report `Assert.Equal() Failure`; the temporary file was then removed. `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore` exited 0 after a Release build with 0 warnings/errors and 86 discovered tests; adding `-RunLegacySmoke` also exited 0 with all 428 retained smoke assertions passing; `git diff --check` passed.
Manual checks: assertion-call inventory counts 180 `AssertTrue` references including its helper, 237 `AssertEqual` invocations, and 13 `AssertNearlyEqual` references including its helper, yielding 428 calls after excluding the two counted helper declarations. The map totals 428/428 across 24 rows. The new files contain 26 `[Fact]` methods, do not launch the old harness, dynamically locate the repository root, use unique temporary directories, and isolate the only WPF-dependent regression with the `Wpf` trait and an STA host.
Residual risks / follow-up packet: the retained harness intentionally duplicates coverage as an on-demand compatibility oracle and has not been reduced or retired. Its fixed five-second performance characterizations remain machine-sensitive upper bounds; later LUNA-17/LUNA-20 packets own realistic benchmark policy. CI adoption remains LUNA-22.

## LUNA-12

Packet: LUNA-12 - State/source-of-truth contract
Status: complete
Baseline commit: `f5d9c0b` (`test(audit): complete LUNA-11 discovered migration`)
Changed files: `README.md`, `docs/ARCHITECTURE.md`, `docs/RELEASE_NOTES.md`, `docs/STATE_MODEL.md`, `tests/ProjectCostForecast.UnitTests/StateModelCharacterizationTests.cs`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: `docs/STATE_MODEL.md` classifies every `ProjectDataset` root collection and persisted calculated field as authoritative persisted input/history, derived value, presentation preference, or transient UI state. It names one owner for durable project state, live session mutation, calculations, scheduling, migration, storage, preferences, and WPF presentation; maps current identities, mutation entry points, recalculation dependencies, serialization compatibility, and known dirty-tracking seams; and explicitly routes the forecast/transaction and schedule/workspace ownership decisions to LUNA-16A and LUNA-16B. Five focused tests cover nested editable project round-trip, persisted compatibility caches versus ignored derived outputs, monthly forecast dirty/recalculation/audit behavior, contingency item/collection dirty behavior, budget amount synchronization, and schedule output ownership.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore --filter "FullyQualifiedName~StateModelCharacterizationTests"` exited 0 with 5 focused tests passed; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore` exited 0 with 91 discovered tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after the Release build, with 91 discovered tests and all 428 retained smoke assertions passing; `git diff --check` passed.
Manual checks: the round-trip fixture confirmed migration adds a missing management allocation for each configured period, editable root/nested state remains readable, current persisted derived caches remain compatible, and `JsonIgnore` schedule, cost, WPF, and display values are not written. No broad production refactor was introduced.
Residual risks / follow-up packet: direct edits to several non-observable or nested collections still lack generic dirty tracking; persisted derived caches remain until a compatible migration; forecast/transaction canonical ownership is LUNA-16A; schedule/workspace/snapshot subscription and ownership coverage is LUNA-16B; remaining WPF model dependencies are LUNA-14.

## LUNA-13

Packet: LUNA-13 - Remove project/category presentation from persisted models
Status: complete
Baseline commit: `e13690c` (`docs(audit): complete LUNA-12 state model`)
Changed files: `docs/ARCHITECTURE.md`, `docs/RELEASE_NOTES.md`, `docs/STATE_MODEL.md`, `src/ProjectCostForecast.App/Models/ProjectDataset.cs`, `src/ProjectCostForecast.App/ProjectMetadataPresentation.cs`, `src/ProjectCostForecast.App/TaskCategoryEditorWindow.xaml`, `tests/ProjectCostForecast.UnitTests/Luna13ProjectMetadataTests.cs`, `docs/audit/PACKET_LEDGER.md`, and the LUNA status row in `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: `ProjectTaskCode` and `ProjectCategory` now expose only persisted metadata, plain fallback hex inputs, and editability policy helpers; `ProjectDataset.cs` has no WPF media or `MainWindow` reference. `ProjectMetadataPresentation` owns the existing default icon paths, tinting call, named colour labels, selected-colour precedence, and invalid-colour fallbacks. `TaskCategoryEditorWindow` uses direct string bindings through App converters, so edits still refresh from the observable persisted properties without restoring presentation members to the models. Current format and unversioned legacy project fixtures retain their task/category JSON fields and exclude all presentation properties after round-trip.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore --filter "FullyQualifiedName~Luna13ProjectMetadataTests"` exited 0 with 5 focused tests passed; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore` exited 0 with 96 discovered tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 96 discovered tests, and all 428 retained smoke assertions passing; `git diff --check` passed.
Manual checks: the source-level boundary test confirms neither affected model has WPF property types or a `MainWindow` dependency; the STA-hosted converter test materializes default and missing-asset icon sources; colour tests cover valid selection, fallback, white safety fallback, named labels, and unknown hex labels. The fixture theory covers both `current-v1.json` and `legacy-unversioned.json`.
Residual risks / follow-up packet: `ForecastLine`, `WorkspaceViewTab`, and other summary/schedule candidates still carry WPF presentation debt and are owned by LUNA-14. The fallback defaults remain ignored runtime strings assigned by the view model; a future Core split should keep those inputs separate from WPF visual materialization.

## LUNA-14

Packet: LUNA-14 - Remove WPF types from forecast and summary domain candidates
Status: complete
Baseline commit: `6acf37d` (`refactor(audit): complete LUNA-13 metadata presentation`)
Changed files: `docs/ARCHITECTURE.md`, `docs/RELEASE_NOTES.md`, `docs/STATE_MODEL.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, `src/ProjectCostForecast.App/App.xaml`, `src/ProjectCostForecast.App/MainWindow.xaml`, `src/ProjectCostForecast.App/TaskCategoryEditorWindow.xaml`, `src/ProjectCostForecast.App/Models/AppUserPreferences.cs`, `src/ProjectCostForecast.App/Models/ForecastLine.cs`, `src/ProjectCostForecast.App/Models/Summaries.cs`, `src/ProjectCostForecast.App/Presentation/AccountingNoDecimalsConverter.cs`, `src/ProjectCostForecast.App/Presentation/ForecastGroupSummaryConverter.cs`, `src/ProjectCostForecast.App/Presentation/GridColumnHighlightState.cs`, `src/ProjectCostForecast.App/Presentation/GridColumnPresentationState.cs`, `src/ProjectCostForecast.App/Presentation/GridColumnRoleState.cs`, `src/ProjectCostForecast.App/Presentation/GridHoverState.cs`, `src/ProjectCostForecast.App/Presentation/GridSelectionStatus.cs`, `src/ProjectCostForecast.App/Presentation/GridSelectionVisualState.cs`, `src/ProjectCostForecast.App/Presentation/NonNegativeDoubleConverter.cs`, `src/ProjectCostForecast.App/Presentation/ScheduleGridConverters.cs`, `src/ProjectCostForecast.App/Presentation/SummaryPresentationModels.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `tests/ProjectCostForecast.UnitTests/Luna11CalculationCoverageTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna14ArchitectureTests.cs`, and the moved/deleted model-folder converter and grid-state files.
Characterization evidence: forecast and summary model candidates no longer reference WPF, controls, dialogs, or `MainWindow`. Forecast-row brushes were removed as unused WPF state; `KpiPill`, `WorkspaceViewTab`, `ForecastMonthColumnDefinition`, attached grid state, and converters now live in the App presentation folder. Persisted `MonthlyForecast` amount/lock state, category summaries, and project-local workspace layouts retain their JSON shape.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj --no-restore /p:BaseOutputPath='artifacts/luna14-test/' --filter "FullyQualifiedName~Luna14ArchitectureTests"` exited 0 with 7 focused tests passed; the same command without a filter exited 0 with 103 discovered tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 103 discovered tests, and all 428 retained smoke assertions passing; `git diff --check` passed.
Manual checks: the architecture test scans every C# file in `Models`, and its deliberate `System.Windows` negative control fails the rule as intended. STA-hosted checks preserve grid role/highlight/selection state, forecast-column colours and separators, KPI comparison visibility, and missing workspace-icon fallback. Both current-format and unversioned legacy fixtures round-trip forecast amounts, lock state, category summaries, and workspace icon/layout values without WPF properties.
Residual risks / follow-up packet: persisted forecast/summary derived caches remain for compatibility and canonical forecast/transaction ownership remains LUNA-16A. Project-local workspace ownership and nested dirty tracking remain LUNA-16B; dialog extraction remains LUNA-15A/LUNA-15B; the eventual Core assembly split remains a later packet.

## LUNA-15A

Packet: LUNA-15A - Decouple open/save use cases from WPF dialogs
Status: complete
Baseline commit: `0d00aff` (`refactor(audit): complete LUNA-14 presentation boundary`)
Changed files: `docs/ARCHITECTURE.md`, `docs/RELEASE_NOTES.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, `src/ProjectCostForecast.App/Presentation/ProjectFileDialogAdapters.cs`, `src/ProjectCostForecast.App/Services/ProjectFileWorkflow.cs`, `src/ProjectCostForecast.App/Services/ServiceContracts.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModelDependencies.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ProjectIO.cs`, and `tests/ProjectCostForecast.UnitTests/Luna15FileWorkflowTests.cs`.
Characterization evidence: `ProjectFileWorkflow` returns explicit succeeded/cancelled/failed results and owns project-file validation, open/save path selection, content-hash conflict decisions, Reload, Save As, and pre-save mutation rollback. `WpfProjectFilePicker` and `WpfProjectPrompt` contain the WPF dialog/message-box construction. The view model applies a loaded session only after a successful workflow result, changes path/revision and clears dirty state only after a successful save, and rolls back the save audit event on failure or conflict.
Commands and results: `dotnet build ProjectCostForecast.sln -c Release --no-restore` exited 0 with 0 warnings and 0 errors; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore --filter "FullyQualifiedName~Luna15FileWorkflowTests"` exited 0 with 10 tests passed; the full discovered suite exited 0 with 113 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 113 discovered tests, and all 428 retained smoke assertions passing; `git diff --check` passed.
Manual checks: headless tests cover open prompt/file-picker cancellation, open validation and I/O failure, successful session replacement, Save As cancellation, save validation and I/O failure, and stale-file Cancel/Reload/Save As. The I/O failure test proves the save audit event is removed; conflict tests prove the active path and dirty state remain unchanged on Cancel and are updated only on Reload or successful Save As.
Residual risks / follow-up packet: backup restore remains on its existing WPF recovery path; import/export, preview, and unmatched-item decisions remain direct view-model UI concerns owned by LUNA-15B. The WPF shell and recovery close/discard prompts remain their existing UI concerns; LUNA-15A routes project open/save workflow decisions through the application boundary.

## LUNA-15B

Packet: LUNA-15B - Decouple import/export use cases from WPF dialogs
Status: complete
Baseline commit: `82f77e8` (`refactor(audit): complete LUNA-15A file workflow`)
Changed files: `docs/ARCHITECTURE.md`, `docs/RELEASE_NOTES.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, `src/ProjectCostForecast.App/Presentation/ImportExportDialogAdapters.cs`, `src/ProjectCostForecast.App/Services/ServiceContracts.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModelDependencies.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ProjectIO.cs`, and `tests/ProjectCostForecast.UnitTests/Luna15ImportExportWorkflowTests.cs`.
Characterization evidence: `IImportExportInteraction` supplies generic open/save paths, mapping decisions, auto-create preview decisions, unmatched-import review, and notifications. `WpfImportExportInteraction` contains the WPF dialogs and windows. Import parsing remains in `CsvTransactionService`; mapping and preview changes are applied to a cloned dataset and committed only after the final validation, while preview cancellation records unmatched combinations as the explicit user-selected outcome.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore --filter "FullyQualifiedName~Luna15ImportExportWorkflowTests" /p:BaseOutputPath='artifacts/luna15b-test/'` exited 0 with 10 focused tests passed; the full discovered suite exited 0 with 123 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release solution build with 0 warnings/errors, 123 discovered tests, and all 428 retained smoke assertions passing; `git diff --check` passed.
Manual checks: source-level coverage confirms `MainWindowViewModel.ProjectIO.cs` no longer constructs file dialogs or import/mapping/preview/unmatched windows. Headless tests cover picker cancellation, unsupported and malformed imports, validation failure, mapping cancellation/acceptance, preview cancellation/edits, export cancellation/failure, and unmatched review routing without creating a WPF window.
Residual risks / follow-up packet: drag-and-drop extension messaging, new-month prompts, saved-month viewers, and close/discard prompts remain WPF shell concerns. Canonical forecast/transaction ownership and the broader Core/Application assembly split remain LUNA-16A and later architecture work.

## LUNA-16A

Packet: LUNA-16A - Canonical forecast/transaction state and derived totals
Status: complete
Baseline commit: `4d8081a` (`refactor(audit): complete LUNA-15B import export workflow`)
Changed files: `docs/ARCHITECTURE.md`, `docs/RELEASE_NOTES.md`, `docs/STATE_MODEL.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, `src/ProjectCostForecast.App/Models/ProjectDataset.cs`, `src/ProjectCostForecast.App/Services/InitialCostLoadService.cs`, `src/ProjectCostForecast.App/Services/ProjectDatasetMigrationPipeline.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Budget.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Dataset.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ProjectIO.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.SavedMonths.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.TotalsAndPreferences.cs`, and `tests/ProjectCostForecast.UnitTests/Luna16CanonicalStateTests.cs`.
Characterization evidence: `ProjectDataset.ForecastLines` and `ProjectDataset.Transactions` now use dataset-owned observable collections exposed directly by the live view model, removing the prior two-way list-copy boundary. Forecast edits recalculate line and category values from canonical inputs and preserve the existing dirty/audit gateway. Category-summary compatibility data is rebuilt on load and before application saves; resource and fiscal projections continue to derive from the same financial inputs. Initial cost-load snapshots calculate on a cloned dataset, avoiding temporary replacement of live transactions. Saved-month viewing uses a separate display collection and restores the live collection and object identities on close.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~Luna16CanonicalStateTests"` exited 0 with 5 focused tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 128 discovered tests, and all 428 retained smoke assertions passing; `git diff --check` exited 0.
Manual checks: the canonical-state tests prove reference identity between the active dataset and view-model financial collections, stale category caches are replaced by calculated values, legacy-fixture totals match a fresh calculation, and saved-month projection switching leaves live rows unchanged. The JSON remains array-compatible; the observable collection type is an in-process ownership detail.
Residual risks / follow-up packet: persisted forecast-line and category-summary derived caches remain for backward compatibility and require a future versioned migration before removal. Direct mutation of the public raw transaction collection remains unsupported outside the import gateway because `CostTransaction` has no property-change notifications. Schedule, workspace, snapshot, and nested subscription ownership remain LUNA-16B.

## LUNA-16B

Packet: LUNA-16B - Canonical schedule/workspace/snapshot state
Status: complete
Baseline commit: `1121c82` (`refactor(audit): complete LUNA-16A canonical financial state`)
Changed files: `docs/ARCHITECTURE.md`, `docs/RELEASE_NOTES.md`, `docs/STATE_MODEL.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, `src/ProjectCostForecast.App/Models/CollectionSubscriptionTracker.cs`, `src/ProjectCostForecast.App/Models/ProjectDataset.cs`, `src/ProjectCostForecast.App/Models/Schedule.cs`, `src/ProjectCostForecast.App/Presentation/SummaryPresentationModels.cs`, `src/ProjectCostForecast.App/Services/InitialCostLoadService.cs`, `src/ProjectCostForecast.App/Services/ProjectDatasetMigrationPipeline.cs`, `src/ProjectCostForecast.App/Services/SchedulingService.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Dataset.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.SavedMonths.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Schedule.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.TotalsAndPreferences.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Workspaces.cs`, and `tests/ProjectCostForecast.UnitTests/Luna16BStateOwnershipTests.cs`.
Characterization evidence: schedule activities, calendars, baselines, saved-month history, and project-local workspace layouts now have explicit dataset-owned collection boundaries. `CollectionSubscriptionTracker` replaces item subscriptions by reference, attaches each current item once, detaches old collections, and stops callbacks after disposal. Workspace presentation tabs remain an explicit WPF projection over project-local layouts; application preferences remain outside the project dirty boundary.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj --no-restore /p:BaseOutputPath='artifacts/luna16b-test/' --filter "FullyQualifiedName~Luna16BStateOwnershipTests"` exited 0 with 4 focused tests passed; the full discovered suite exited 0 with 132 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 132 discovered tests, and all 428 retained smoke assertions passing; `git diff --check` passed.
Manual checks: focused headless regressions prove dataset/view-model collection identity, idempotent replacement and stale callback detachment, dirty coverage for schedule/snapshot/workspace edits, application-preference separation, and detachment of old project schedule/workspace objects after reload. The retained console smoke gate confirms schedule recalculation, baseline editing, snapshot creation, and workspace behavior remain compatible.
Residual risks / follow-up packet: direct mutation inside nested working-day, holiday, extra-work-day, baseline-entry, snapshot, and workspace canvas lists remains outside generic observable tracking and is covered by explicit gateways where supported. Refresh coalescing and preservation of richer WPF view state remain LUNA-17; deeper nested tracking remains a later state-model follow-up.

## LUNA-17

Packet: LUNA-17 - Refresh coordinator and UI-state preservation
Status: complete
Baseline commit: `664920b` (`refactor(audit): complete LUNA-16B canonical schedule state`)
Changed files: `src/ProjectCostForecast.App/Services/RefreshCoordinator.cs`, `src/ProjectCostForecast.App/MainWindow.GridRefreshState.cs`, `src/ProjectCostForecast.App/MainWindow.GridBuilders.cs`, `src/ProjectCostForecast.App/MainWindow.Budget.cs`, `src/ProjectCostForecast.App/MainWindow.WorkspaceColumnState.cs`, `src/ProjectCostForecast.App/MainWindow.xaml.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Refresh.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ViewsAndFilters.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.TotalsAndPreferences.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.TaskCategories.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Pivots.cs`, `tests/ProjectCostForecast.UnitTests/Luna17RefreshCoordinatorTests.cs`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, and `docs/audit/PACKET_LEDGER.md`.
Characterization evidence: `RefreshCoordinator` is the single merge boundary for calculation, calculated views, collection views, pivots, totals, ledger, grouping, and filter-list work. Requests union their explicit projection flags and boolean requirements; spreadsheet edit batches defer dependent refreshes until the outer batch closes. The forecast grid captures stable item and column keys, multi-row selection, scroll offsets, active editor text, current cell, and task-group expansion before dynamic columns are replaced, then restores only identities that remain available. Filter values remain view-model state and are not reset by calculated collection replacement.
Commands and results: `dotnet build ProjectCostForecast.sln -c Debug --no-restore /p:BaseOutputPath='artifacts/luna17-build-3/'` exited 0 with 0 warnings and 0 errors; the focused LUNA-17 suite exited 0 with 3 tests passed; the full Debug discovered suite exited 0 with 135 tests passed; Release verification with `-NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 135 discovered tests, and all 428 retained smoke assertions passing; `git diff --check` passed.
Manual checks: a full recalculation records exactly one calculation, calculated-view rebuild, collection-view refresh, raw-transaction pivot rebuild, totals notification, and ledger notification. Repeated property/collection notifications use one dispatcher column rebuild per grid. The focused tests prove request merging, batch holding, stable line/resource/filter identity, and no duplicate full-refresh calculation/pivot path.
Residual risks / follow-up packet: the coordinator is intentionally an in-process UI refresh boundary; child-window cancellation and async exception ownership remain LUNA-18B. Broader performance baselines and stress targets remain LUNA-20.

## LUNA-18A

Packet: LUNA-18A - Main window lifecycle and subscription ownership
Status: complete
Baseline commit: `31394ab` (`refactor(audit): complete LUNA-17 refresh coordinator and UI-state preservation`)
Changed files: `src/ProjectCostForecast.App/MainWindow.xaml.cs`, `src/ProjectCostForecast.App/MainWindow.Lifecycle.cs`, `src/ProjectCostForecast.App/MainWindow.Gantt.cs`, `src/ProjectCostForecast.App/MainWindow.GridBuilders.cs`, `src/ProjectCostForecast.App/MainWindow.GridFilters.cs`, `src/ProjectCostForecast.App/MainWindow.ForecastGridInteraction.cs`, `src/ProjectCostForecast.App/MainWindow.GridRefreshState.cs`, `src/ProjectCostForecast.App/MainWindow.ManagementResources.cs`, `src/ProjectCostForecast.App/MainWindow.SpreadsheetGridInteraction.cs`, `src/ProjectCostForecast.App/MainWindow.WorkspaceColumnState.cs`, `src/ProjectCostForecast.App/MainWindow.WorkspacePanels.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Lifecycle.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Refresh.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Schedule.cs`, `tests/ProjectCostForecast.UnitTests/Luna18ALifecycleTests.cs`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, and this ledger.
Characterization evidence: the audit found duplicate Gantt lifecycle registrations alongside the original MainWindow handlers, visual subscriptions without an unload owner, routed handlers attached with `AddHandler`, and dispatcher callbacks that could run after close. The named lifecycle boundary now owns attach/detach; repeated loads reattach without repeating initial column rebuilds; data-context replacement rewires the old view model before refreshing the new one; and every retained window callback captures a lifetime generation.
Commands and results: `dotnet build ProjectCostForecast.sln -c Debug --no-restore /p:BaseOutputPath='artifacts/luna18a-build-2/'` exited 0 with 0 warnings and 0 errors; the focused LUNA-18A suite exited 0 with 4 tests passed; the full Debug discovered suite exited 0 with 139 tests passed; Release verification with `-NoRestore -RunLegacySmoke` and `git diff --check` are the final packet gates.
Manual checks: routed handlers are removed with `RemoveHandler` using the same event/delegate pair used by `AddHandler`; forecast, management-resource, and schedule scroll subscriptions are detached on unload; pending refresh and schedule dispatcher operations are aborted when the view model is disposed; and a queued `RefreshCoordinator` callback is a no-op after disposal.
Residual risks / follow-up packet: child-window async work, cancellation/error observation, and the WPF binding-error trace gate remain LUNA-18B/LUNA-18C. Shared interaction duplication remains LUNA-19A to LUNA-19C, and measured performance baselines remain LUNA-20.

## LUNA-18B

Packet: LUNA-18B - Child-window async, cancellation, and events
Status: complete
Baseline commit: `2e9020a` (`refactor(audit): complete LUNA-18A window lifecycle ownership`)
Changed files: `src/ProjectCostForecast.App/ScheduleComparisonWindow.cs`, `src/ProjectCostForecast.App/Services/ObservedAsyncOperation.cs`, `src/ProjectCostForecast.App/MainWindow.ScheduleCommands.cs`, `src/ProjectCostForecast.App/MainWindow.Lifecycle.cs`, `tests/ProjectCostForecast.UnitTests/Luna18BChildWindowTests.cs`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, and this ledger.
Characterization evidence: schedule comparison used an `async` lambda passed as `Action` to `Dispatcher.BeginInvoke`; its cancellation source was cancelled but not disposed or awaited, and a close/request race could reach the summary or grids after the window was no longer active. The parent also used an anonymous child `Closed` subscription. Other child-window dispatcher calls were reviewed as synchronous positioning/editing work without background tasks.
Commands and results: `dotnet build ProjectCostForecast.sln -c Debug --no-restore /p:BaseOutputPath='artifacts/luna18b-build-2/'` exited 0 with 0 warnings and 0 errors; the focused LUNA-18B suite exited 0 with 4 tests passed; the full Debug discovered suite and Release verification with `-NoRestore -RunLegacySmoke` are the final packet gates; `git diff --check` passed.
Manual checks: the schedule comparison refresh set is observed with `await`; a newer request cancels the previous request; close cancels and awaits all active refresh tasks, detaches named handlers, and disposes both cancellation sources; UI writes re-check request/window lifetime; cancellation is not diagnosed as an error; and non-cancellation exceptions are diagnosed before the user-facing failure text.
Residual risks / follow-up packet: WPF binding-error capture is complete in LUNA-18C. Shared grid panning, canvas interaction, and forecast curve duplication remain LUNA-19A to LUNA-19C; measured performance baselines remain LUNA-20.

## LUNA-18C

Packet: LUNA-18C - WPF binding-error gate
Status: complete
Baseline commit: `d014093` (`refactor(audit): complete LUNA-18B child-window async ownership`)
Changed files: `src/ProjectCostForecast.App/MainWindow.xaml.cs`, `src/ProjectCostForecast.App/Services/WpfBindingErrorCapture.cs`, `tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj`, `tests/ProjectCostForecast.UnitTests/Luna18CBindingErrorTests.cs`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, and this ledger.
Characterization evidence: the test-scoped listener attaches to `PresentationTraceSources.DataBindingSource`, captures WPF error events with a surface prefix, and restores the prior listener/trace configuration on disposal. A synthetic missing-path event proves the captured diagnostic retains both the binding path and surface name. The representative STA smoke path constructs the main window, visits the main forecast, resources, ledger, schedule, monthly report, and saved-month surfaces, returns to the current month, and closes cleanly.
Commands and results: `dotnet build tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Debug --no-restore /p:BaseOutputPath='artifacts/luna18c-build/'` exited 0 with 0 warnings and 0 errors; `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Debug --no-build --no-restore --filter "FullyQualifiedName~Luna18CBindingErrorTests"` exited 0 with 3 focused tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build, 146 discovered tests, and all 428 retained smoke assertions passing; the representative smoke test reported zero unexpected WPF binding errors; `git diff --check` passed.
Manual checks: the test host links the app’s compiled icon/background resources so application-relative WPF image URIs resolve under the test assembly; the forecast-group icon lookup is assembly-qualified for deterministic resolution. No framework binding noise or application binding error was observed, so no allow-list was added and no global trace suppression was introduced.
Residual risks / follow-up packet: the gate covers the agreed representative main-window surfaces; newly introduced child windows or controls should add their own scoped smoke coverage. Shared grid panning, canvas interaction, and forecast curve duplication remain LUNA-19A to LUNA-19C; measured performance baselines remain LUNA-20.

## LUNA-19A

Packet: LUNA-19A - Shared right-click grid panning behaviour
Status: complete
Baseline commit: `aea2859` (`refactor(audit): complete LUNA-18C WPF binding-error gate`)
Changed files: `src/ProjectCostForecast.App/Presentation/RightClickGridPanBehavior.cs`, `src/ProjectCostForecast.App/Presentation/RightClickGridPanSession.cs`, `src/ProjectCostForecast.App/Properties/InternalsVisibleTo.cs`, `src/ProjectCostForecast.App/MainWindow.xaml.cs`, `src/ProjectCostForecast.App/MainWindow.GridFilters.cs`, `src/ProjectCostForecast.App/MainWindow.Lifecycle.cs`, `src/ProjectCostForecast.App/MainWindow.WindowChrome.cs`, `src/ProjectCostForecast.App/MainWindow.ForecastGridInteraction.cs`, `src/ProjectCostForecast.App/MainWindow.GridBuilders.cs`, `src/ProjectCostForecast.App/MainWindow.SpreadsheetGridInteraction.cs`, `src/ProjectCostForecast.App/CostCenterMappingWindow.cs`, `src/ProjectCostForecast.App/TaskCategoryEditorWindow.xaml`, `src/ProjectCostForecast.App/TaskCategoryEditorWindow.xaml.cs`, `tests/ProjectCostForecast.UnitTests/Luna19ARightClickGridPanTests.cs`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, and this ledger.
Characterization evidence: MainWindow, CostCenterMappingWindow, and TaskCategoryEditorWindow each had a local right-button panning state machine. Their common semantics were a 6px threshold, opposite-direction scrolling, column-header exclusion, and drag-time handling, while capture-loss and context-menu behavior differed. The attached behavior now owns one per-grid session and preserves the existing grid selection, row-resize, scrollbar, and header actions around it.
Commands and results: `dotnet build ProjectCostForecast.sln -c Debug --no-restore /p:BaseOutputPath='artifacts/luna19a-build/'` exited 0 with 0 warnings and 0 errors; the focused `Luna19ARightClickGridPanTests` suite exited 0 with 5 tests passed; the full Debug discovered suite exited 0 with 151 tests passed; Release verification with `-NoRestore -RunLegacySmoke` and `git diff --check` are the final packet gates.
Manual checks: click movement below 6px remains available for context menus; movement at or beyond the threshold captures the grid, clamps each enabled axis to its `ScrollViewer` range, and suppresses the matching right-button-up route. Column headers and scrollbar parts do not start a pan, and an attached behavior is detached with the MainWindow visual lifecycle.
Residual risks / follow-up packet: shared report-canvas/color utilities and forecast-curve duplication remain LUNA-19B/LUNA-19C; measured performance baselines remain LUNA-20.

## LUNA-19B

Packet: LUNA-19B - Shared report-canvas interaction and colour utilities
Status: complete
Baseline commit: `c03cfde` (`refactor(audit): complete LUNA-19A shared grid panning`)
Changed files: `src/ProjectCostForecast.App/Presentation/ColorValue.cs`, `src/ProjectCostForecast.App/Presentation/ReportCanvasInteraction.cs`, `src/ProjectCostForecast.App/BrushFactory.cs`, `src/ProjectCostForecast.App/MonthlyReportCanvasObjects.cs`, `src/ProjectCostForecast.App/MonthlyReportCharts.cs`, `src/ProjectCostForecast.App/MonthlyReportCanvasInteractions.cs`, `src/ProjectCostForecast.App/ProjectMetadataPresentation.cs`, `src/ProjectCostForecast.App/BuiltInIconPickerWindow.xaml.cs`, `src/ProjectCostForecast.App/CostCenterMappingWindow.cs`, `src/ProjectCostForecast.App/ImportAutoCreatePreviewWindow.cs`, `src/ProjectCostForecast.App/MainWindow.ColumnMenus.cs`, `src/ProjectCostForecast.App/MainWindow.Gantt.cs`, `src/ProjectCostForecast.App/MainWindow.GridBuilders.cs`, `src/ProjectCostForecast.App/MainWindow.IconPicker.cs`, `src/ProjectCostForecast.App/SavedMonthSnapshotWindow.cs`, `src/ProjectCostForecast.App/ScheduleCalendarWindow.cs`, `src/ProjectCostForecast.App/TaskCategoryEditorWindow.xaml.cs`, `src/ProjectCostForecast.App/HeaderColorPickerPopup.xaml.cs`, `src/ProjectCostForecast.App/Presentation/GridColumnPresentationState.cs`, `tests/ProjectCostForecast.UnitTests/Luna19BReportCanvasTests.cs`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, and this ledger.
Characterization evidence: the two monthly-report card types previously owned parallel drag fields, handlers, button exclusions, canvas clamping, layout synchronization, selection branches, and z-index resets. The shared controller now reads pointer positions in canvas coordinates, preserves the original z-index after drag completion or capture loss, and calls the existing position-change gateway. Both cards implement the same host contract, and placement/containment uses the same bounded-position helper.
Colour evidence: persisted and UI hex values now pass through the WPF-free `ColorValueParser`, which normalizes supported RGB/RGBA hex forms; `ColorPalette` owns the shared metadata labels and icon options; and `BrushFactory` is the WPF conversion boundary. The main grid, task/category editor, and header picker use one frozen default header gradient. Direct `ColorConverter` call sites were removed from the app surfaces in scope.
Commands and results: the focused `Luna19BReportCanvasTests` suite exited 0 with 15 tests passed; the full Debug discovered suite exited 0 with 166 tests passed; the Release verifier and retained legacy smoke gate are the final packet gates; `git diff --check` passed.
Manual checks: the pure parser covers three-, four-, six-, and eight-digit hex, invalid values, alpha conversion, labels, and fallback behavior. WPF checks prove the shared gradient is frozen and has the established three stops, brush creation remains at the presentation boundary, and source-contract checks confirm both cards use the shared controller without duplicated header drag handlers.
Residual risks / follow-up packet: forecast-curve formula duplication and preview/application parity remain LUNA-19C; realistic end-to-end performance baselines remain LUNA-20.

## LUNA-19C

Packet: LUNA-19C - Canonical forecast-curve math
Status: complete
Baseline commit: `bae0f6d` (`refactor(audit): complete LUNA-19B report canvas utilities`)
Changed files: `src/ProjectCostForecast.App/ForecastCurveWindow.cs`, `src/ProjectCostForecast.App/Services/ForecastCurveService.cs`, `tests/ProjectCostForecast.UnitTests/Luna19CForecastCurveTests.cs`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, and this ledger.
Characterization evidence: the service and editor preview previously used separate profile formulas, S-curve steepness, rounding precision, and residual placement. `ForecastCurveMath.Distribute` now owns the pure normalized profile weights, cent rounding, and largest-period residual allocation. `ForecastCurveService` delegates committed editable-month allocation to it; `ForecastCurvePresets` delegates built-in previews, normalized shape capture, and user-shape application to the same boundary. Cumulative-marker movement and interactive smoothing remain separate display/interaction math.
Commands and results: `dotnet build ProjectCostForecast.sln -c Debug --no-restore /p:BaseOutputPath='artifacts/luna19c-build-2/'` exited 0 with 0 warnings and 0 errors; the focused `Luna19CForecastCurveTests` suite exited 0 with 9 tests passed; the full Debug discovered suite exited 0 with 175 tests passed; Release verification with `-NoRestore -RunLegacySmoke` and `git diff --check` are the final packet gates.
Manual checks: representative preview values match service allocations for S-curve, bell/lazy, front-loaded, and back-loaded profiles; zero-period, one-period, invalid-profile, negative-total, and cent-residual cases preserve the documented rules; existing curves remain identity projections; and the source contract confirms no duplicate profile weight, logistic, or residual formula remains in either adapter.
Residual risks / follow-up packet: realistic startup, load, save, import, refresh, workspace, schedule, and repeated open/close performance baselines remain LUNA-20.

## LUNA-20

Packet: LUNA-20 - Realistic performance baseline and evidence-led fixes
Status: complete
Baseline commit: `39c503f` (`refactor(audit): complete LUNA-19C forecast curve math`)
Changed files: `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Pivots.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ProjectIO.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.Refresh.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.TotalsAndPreferences.cs`, `src/ProjectCostForecast.App/ViewModels/MainWindowViewModel.ViewsAndFilters.cs`, `tests/ProjectCostForecast.Tests/Program.cs`, `tests/ProjectCostForecast.Tests/Luna20PerformanceRunner.cs`, `tests/ProjectCostForecast.UnitTests/Luna20PerformanceContractTests.cs`, `scripts/verify-performance.ps1`, `docs/audit/LUNA-20-PERFORMANCE-BASELINE.json`, `docs/audit/LUNA-20-PERFORMANCE-POST-CHANGE.json`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, and `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: the opt-in headless runner creates deterministic small, normal, and stress datasets of 50/500/18/100, 500/20,000/36/1,000, and 2,000/100,000/60/2,500 forecast lines/transactions/periods/schedule activities. It records project JSON and CSV byte sizes before timing startup/load/save/import, full recalculation, schedule calculation, normal grid-edit refresh, workspace switching, and repeated open/close memory work. The baseline and post-change artifacts use Windows 10.0.19045, .NET 8.0.29, win-x64 Release, 16 processors, three timing iterations, and five memory cycles; the post report records 21 scenarios and all workload correctness checks.
Commands and results: `dotnet build ProjectCostForecast.sln -c Debug --no-restore /p:BaseOutputPath='artifacts/luna20-build/'` exited 0 with 0 warnings and 0 errors; the focused `Luna20PerformanceContractTests` suite exited 0 with 1 test passed; the full Debug discovered suite exited 0 with 176 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify-performance.ps1' -OutputPath 'docs/audit/LUNA-20-PERFORMANCE-POST-CHANGE.json' -BaselinePath 'docs/audit/LUNA-20-PERFORMANCE-BASELINE.json' -EnforceRegression -Iterations 3 -MemoryCycles 5` exited 0 with a Release build at 0 warnings/errors, all 21 workload checks passing, and no median scenario above baseline plus `max(50 ms, 25%)`; the reports retain p95 evidence and runs with at least 20 samples enforce p95; `git diff --check` passed.
Manual checks: the normal grid-edit refresh p95 fell from 228.502 ms to 202.276 ms. Its refresh diagnostics recorded five calculated-view, collection-view, totals, and ledger phases but only one raw-transaction-pivot phase for initial construction; the five forecast-only edits did not rebuild that unrelated pivot. Manual full recalculation and import retain the raw-pivot dependency, and the focused contract test proves the forecast-only counter behavior.
Residual risks / follow-up packet: timing values are hardware/configuration-specific and the performance verifier remains an explicit opt-in gate; rerun baseline and post-change artifacts on materially different hardware. Calculated-view construction remains the largest measured grid-edit phase and is intentionally left for a separately characterized optimization. Dependency locking and audit remain LUNA-21; CI enforcement remains LUNA-22.

## LUNA-21

Packet: LUNA-21 - Deterministic and audited dependencies
Status: complete
Baseline commit: `2bdf5a5` (`perf(audit): complete LUNA-20 performance baseline`)
Changed files: `Directory.Build.props`, `src/ProjectCostForecast.App/ProjectCostForecast.App.csproj`, `src/ProjectCostForecast.App/packages.lock.json`, `tests/ProjectCostForecast.Tests/packages.lock.json`, `tests/ProjectCostForecast.UnitTests/packages.lock.json`, `scripts/verify.ps1`, `scripts/audit-dependencies.ps1`, `tests/ProjectCostForecast.UnitTests/Luna21DependencyContractTests.cs`, `docs/audit/LUNA-21-DEPENDENCY-AUDIT.json`, `docs/RELEASE_CHECKLIST.md`, `README.md`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, and `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`.
Characterization evidence: the production ClosedXML reference is updated from 0.105.0 to the compatible 0.105.1 patch. Root restore policy enables lock files and NuGet audit mode `all`; each of the three projects has a committed lock file, and the verification script restores with `--locked-mode`. The dependency audit script inventories direct and transitive packages for the application, legacy smoke harness, and discovered test project, queries NuGet advisories, and can fail on any reported vulnerability. The captured graph contains 22 packages and zero vulnerability records. License metadata from the resolved nuspec files is reviewed in `docs/RELEASE_CHECKLIST.md`: production packages are MIT except `SixLabors.Fonts` (Apache-2.0), test packages are MIT or Apache-2.0, and the two legacy URL-only declarations retain their URLs for manual notice review.
Commands and results: network-enabled `dotnet restore ProjectCostForecast.sln --locked-mode` exited 0; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\audit-dependencies.ps1' -NoRestore -FailOnVulnerability -OutputPath 'docs/audit/LUNA-21-DEPENDENCY-AUDIT.json'` exited 0 with 22 packages inventoried and 0 vulnerability records; `dotnet build ProjectCostForecast.sln -c Debug --no-restore /p:BaseOutputPath='artifacts/luna21-build/'` exited 0 with 0 warnings and 0 errors; the focused dependency-contract suite exited 0 with 2 tests passed; the full Debug discovered suite exited 0 with 178 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -RunLegacySmoke` exited 0 after locked restore, a Release build with 0 warnings/errors, 178 discovered tests, and all 428 retained smoke assertions; `git diff --check` passed.
Manual checks: the locked restore was repeated after lock-file generation and reported all projects up to date. `dotnet list package --include-transitive --format json --no-restore` resolved the committed application graph, while the network-backed `--vulnerable --include-transitive` query returned no vulnerability entries. The license table records package use and declared metadata without adding a production dependency; no major or unrelated transitive upgrade was forced.
Residual risks / follow-up packet: advisory results are point-in-time and depend on the configured NuGet source; rerun the audit for each release. The two URL-only license declarations still require preserving upstream notices in the release channel. CI invocation of the locked restore and dependency audit, tracked-release/bundled-data hygiene, and secret scanning remain LUNA-22.

## LUNA-22

Packet: LUNA-22 - CI, release artifacts, and bundled-data hygiene
Status: complete
Baseline commit: `2afc35c` (`chore(audit): complete LUNA-21 dependency controls`)
Changed files: `.github/workflows/verify.yml`, `.gitignore`, `src/ProjectCostForecast.App/ProjectCostForecast.App.csproj`, `scripts/scan-secrets.ps1`, `scripts/verify-performance.ps1`, `tests/ProjectCostForecast.UnitTests/Luna22RepositoryHygieneTests.cs`, `docs/audit/LUNA-22-BUNDLED-DATA-REVIEW.md`, `docs/audit/F-13-CLEANUP.md`, `docs/RELEASE_CHECKLIST.md`, `README.md`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, and this ledger; the exact 17 cleanup paths are listed in `F-13-CLEANUP.md`.
Characterization evidence: the normal application build now copies only `Data/data_anonymised.xlsx`; the source `SampleData.json` fallback and unused `InitialCostLoad.xlsx` are not normal package content. The bundled-data review records sizes, SHA-256 identity, provenance, and package decisions without printing workbook or project values. Future `release/ProjectCostForecast/` and `Temp/` outputs are ignored. The tracked legacy copies remained untouched at the initial LUNA-22 safe-controls checkpoint; the later delegated F-13 decision authorized their dedicated index-only cleanup.
CI and secret evidence: `.github/workflows/verify.yml` grants `contents: read`, disables checkout credential persistence, runs locked verification, dependency audit, redacted source/history scanning, and the deterministic performance gate, then uploads reports with 14-day retention. `scripts/scan-secrets.ps1` scans tracked text files and every reachable Git commit, reporting only scope/type/file/line locations; the completed scan covered 420 tracked files and 56 commits with zero findings. On 2026-08-29 the user delegated the F-13 decision to Sol, which selected cleanup of the exact 17 legacy paths from the Git index while preserving local copies.
Commands and results: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -RunLegacySmoke` exited 0 after locked restore, a Release build with 0 warnings/errors, 180 discovered tests, and all 428 retained smoke assertions; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify-performance.ps1' -OutputPath 'artifacts/luna22/performance-run-final.json' -BaselinePath 'docs/audit/LUNA-20-PERFORMANCE-BASELINE.json' -EnforceRegression -Iterations 3 -MemoryCycles 5` exited 0 with all 21 workload checks passing and no median scenario above baseline plus `max(50 ms, 25%)`; `git diff --check` passed.
Manual checks: the Release output data directory contains only `data_anonymised.xlsx`; the scanner was also run with `-FailOnMatch`; the dependency audit remains zero-vulnerability from LUNA-21; ignore rules match all 17 future release and scratch paths; `git ls-files` returns none of the 17 cleanup paths; all local copies remain present; and the three source fixtures remain tracked.
Residual risks / follow-up packet: at the LUNA-22 boundary, release-data distribution approval, signed packaging/upgrade/rollback, direct original-workbook importer scope, broader user acceptance, and SOL-00 remained deferred. SOL-00 is recorded below; the formal-production items remain open.

## LUNA-23

Packet: LUNA-23 - Release, recovery, and architecture documentation
Status: complete
Baseline commit: `33cb128` (`chore(audit): complete LUNA-22 safe controls`)
Changed files: `README.md`, `docs/CODEX_START_HERE.md`, `docs/REBUILD_SPEC_FOR_CODEX.md`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, `docs/RELEASE_CHECKLIST.md`, `docs/audit/LUNA-23-RELEASE-TRUTH.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, `tests/ProjectCostForecast.UnitTests/Luna23ReleaseTruthTests.cs`, and this ledger.
Characterization evidence: the release-truth matrix reconciles the current `1.0.1` WPF application with JSON project format v1 and legacy v0 migration, supported CSV/XLSX/XLSM raw transaction imports, anonymised startup data, backup/restore behavior, dirty-close behavior, and the absence of a direct original-workbook project importer. It records the signed-installer/MSIX, rollback, release-data approval, tracked-artifact cleanup, and broader user-acceptance limitations instead of implying they are complete. The historical rebuild specification and start-here guide now label roadmap language and point to the as-built boundary.
Commands and results: isolated Release package build `dotnet build src/ProjectCostForecast.App/ProjectCostForecast.App.csproj -c Release --no-restore /p:BaseOutputPath='artifacts/luna23-package-build/'` exited 0 with 0 warnings/errors; its output `Data` directory contained only `data_anonymised.xlsx`; the focused `Luna23ReleaseTruthTests` suite exited 0 with 2 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 182 discovered tests, and all 428 retained smoke assertions; `git diff --check` passed before final staging.
Manual checks: the release checklist separates ready gates from pending approvals; the close prompt uses Save / Discard / Cancel and the recovery runbook recommends restore to a new path; no server/auth/database or installer/signing claim is made; and the package/data, diagnostic, migration, and rollback boundaries link to source or existing tests.
Residual risks / follow-up packet: release-data owner approval, signed packaging/upgrade/rollback, direct original-workbook importer decision, and additional-project acceptance remain deferred. The separately authorized F-13 cleanup is recorded in its own packet. LUNA-24 owns final clean-checkout closure and the evidence handoff to Sol Ultra.

## LUNA-24

Packet: LUNA-24 - Luna closure and evidence handoff
Status: complete
Baseline commit: `85f3b03` (`docs(audit): prepare LUNA-24 Sol handoff`)
Changed files: `docs/audit/LUNA-24-FINDING-MATRIX.md`, `docs/audit/F-13-CLEANUP.md`, `docs/audit/LUNA_HANDOFF.md`, `tests/ProjectCostForecast.UnitTests/Luna24ClosureEvidenceTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna22RepositoryHygieneTests.cs`, `README.md`, `docs/ARCHITECTURE.md`, `docs/STATE_MODEL.md`, `docs/RELEASE_NOTES.md`, `docs/RELEASE_CHECKLIST.md`, `docs/audit/LUNA-23-RELEASE-TRUTH.md`, `docs/VIBE_CODEBASE_AUDIT_AND_REMEDIATION_PLAN.md`, and this ledger; the exact 17 cleanup paths are listed in `F-13-CLEANUP.md`.
Characterization evidence: the matrix maps every F-00 through F-20 finding to a fixing packet or explicit limitation. It records no unresolved P0, marks F-13 fixed, and lists the exact cleanup scope as 16 `release/ProjectCostForecast/` files plus one `Temp` file. The handoff gives Sol Ultra the required reading, independent verification obligations, clean-build evidence, and resolved-risk register; it requires independent verification of the cleanup commit and does not claim SOL-00 acceptance.
Commands and results: the pre-cleanup checkout was clean; the F-13 index-only cleanup was performed with `git rm --cached` and exact paths; the exact-path check reports 17 ignored, 0 tracked cleanup paths, 3 tracked source fixtures, and 0 missing local copies; the focused hygiene/closure command passes 5 tests; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -RunLegacySmoke` passes locked restore, a Release build with 0 warnings/errors, 185 discovered tests, and all 428 retained smoke assertions; and `git diff --check` passes.
Manual checks: the matrix contains F-00 through F-20 and marks F-13 fixed; all 17 cleanup paths are ignored, absent from `git ls-files`, and still present locally; the three source fixtures remain tracked; the handoff references the same `verify.ps1 -RunLegacySmoke` command as CI; and generated build/scan evidence remains outside Git.
Residual risks / follow-up packet: at the LUNA-24 handoff boundary, release-data distribution approval, signed packaging/upgrade/rollback, direct original-workbook importer scope, broader user acceptance, and SOL-00 remained pending. SOL-00 is recorded below; the formal-production items remain open.

## LUNA-25

Packet: LUNA-25 - GitHub Actions performance baseline remediation
Status: complete
Input candidate: `7505c80` (`chore(audit): remove F-13 tracked artifacts`)
Fix commit: `2c6cd67` (`perf(audit): add GitHub Windows CI baseline`)
Report: `docs/audit/LUNA-25-CI-PERFORMANCE.md`
Characterization evidence: SOL-FAIL-1 reproduced when GitHub Actions run `33243036341` passed verification, dependency audit, secret scanning, and all 21 performance correctness scenarios but compared its 4-core Windows timings with a 16-core local/developer baseline. The committed CI baseline is semantically identical to the uploaded report after changing only top-level `Mode` to `ci-baseline`; its source SHA-256 and all 21 measurements are preserved. The workflow selects the CI baseline explicitly while the verifier default remains the local/developer baseline.
Commands and results: the two focused LUNA-20/LUNA-25 contracts pass; independent disposable-clone verification of `2c6cd67` passes locked restore, a Release build with 0 warnings/errors, all 186 discovered tests, and all 428 retained smoke assertions; the dependency audit inventories 22 packages with zero vulnerability records; the redacted scan covers 411 tracked files across 60 commits with zero findings; and corrected GitHub Actions run `33243769987` / job `99077522610` passes every workflow step, including the performance regression gate.
Residual risks / follow-up packet: the baseline is valid only for the recorded GitHub Actions Windows profile and must be recaptured under the packet policy after an approved performance change or material runner-profile change. Final acceptance remains SOL-00's independent decision below.

## SOL-00

Packet: SOL-00 - Independent final audit
Status: complete
Candidate commit: `2c6cd67` (`perf(audit): add GitHub Windows CI baseline`)
Report: `docs/audit/SOL-00-FINAL-AUDIT.md`
Independent evidence: baseline ancestry and complete scope review pass; disposable-clean-checkout locked restore, zero-warning Release build, 186 discovered tests, and 428 retained smoke assertions pass; 105 focused critical/WPF tests pass; deliberate failing tests are detected by both local and CI-mode verifier entry points; 22 packages have zero vulnerability records; 411 tracked files across 60 commits have zero secret findings; the normal package contains only `data_anonymised.xlsx`; all 17 F-13 paths are ignored and untracked; isolated local performance passes all 21 scenarios; SOL-FAIL-1 is reproduced and resolved by LUNA-25; corrected GitHub Actions run `33243769987` passes all gates; and release version/signing/installer/recovery claims match the built artifact.
Decision: accepted for codebase-audit closure. Release-data distribution remains a P1 accepted only under a no-distribution mitigation with user/release-data-owner ownership and review on 2026-11-29 or before distribution. Signing/installer, deployment rollback, direct original-workbook import, and broader user acceptance remain formal-production limitations.

## LUNA-26A

Packet: LUNA-26A - Model dependency closure
Status: complete
Baseline commit: `7c42c5f` (`docs(audit): add post-SOL architecture plan`)
Changed files: `src/ProjectCostForecast.App/MainWindow.xaml`, `src/ProjectCostForecast.App/Models/AuditEvent.cs`, `src/ProjectCostForecast.App/Models/ForecastLine.cs`, `src/ProjectCostForecast.App/Models/ProjectDataset.cs`, `src/ProjectCostForecast.App/Presentation/DateTimeDisplayConverter.cs`, `src/ProjectCostForecast.App/Services/FiscalPeriod.cs`, `src/ProjectCostForecast.App/UnmatchedImportWindow.cs`, `tests/ProjectCostForecast.UnitTests/Luna26AModelDependencyTests.cs`, `docs/ARCHITECTURE.md`, `docs/audit/POST-SOL-ARCHITECTURE-REMEDIATION-PLAN.md`, and this ledger.
Characterization evidence: the packet first added tests for the three known model-to-service references (`AuditEvent`/`ProjectDataset` to `Services.DateTimeContract`, and `ForecastMonthlyComment` to `Services.FiscalPeriod`). The final architecture gate scans every C# file below `Models`, strips comments and string/character literals, recognizes using directives and qualified namespace references, and rejects Services, Presentation, ViewModels, WPF, window, control, dialog, message-box, and related presentation dependencies. Deliberate negative controls cover each forbidden layer/direction; comments and literals containing namespace-like prose are accepted. The two timestamp models now retain only persisted instants, and the presentation converter formats them through the documented NZ date/time contract. Forecast comments and the existing public `Services.FiscalPeriod` API share one canonical model-safe parser/sort implementation; service delegation, runtime parity, malformed-label cases, and legacy year-pivot/empty-segment cases prevent divergent behavior. Existing JSON names and persisted values are unchanged.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore --filter "FullyQualifiedName~Luna26A|FullyQualifiedName~Luna14ArchitectureTests|FullyQualifiedName~DateTimeLocaleContractTests|FullyQualifiedName~StateModelCharacterizationTests"` exited 0 with 46 focused tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 213 discovered tests passed, and all 428 retained smoke assertions passing; `git diff --check` passed.
Manual checks: the changed production scope is seven files. `Services/FiscalPeriod.cs` is the parent-requested one-file variance above the six-production-file packet limit so the public API delegates to, rather than duplicates, the canonical model-safe parser. The audit and unmatched-import bindings use `DateTimeDisplayConverter` with `ChangedAt`/`RecordedAt`, and no forbidden dependency reference remains under the model candidate root. No commit or push was performed, per packet instruction.
Residual risks / follow-up packet: this packet deliberately preserves the single WPF production assembly and does not address revision-safe persistence or MainWindow deferred-work lifetime closure; those remain LUNA-26B and LUNA-26C.

## LUNA-26B

Packet: LUNA-26B - Revision-safe persistence boundary
Status: complete
Baseline commit: `db0625d` (`refactor(audit): complete LUNA-26A model boundary`)
Changed files: `src/ProjectCostForecast.App/Services/ProjectFileService.cs`, `src/ProjectCostForecast.App/Services/ProjectFileWriteLock.cs`, `src/ProjectCostForecast.App/Services/ServiceContracts.cs`, `tests/ProjectCostForecast.UnitTests/StaleWriteTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna15FileWorkflowTests.cs`, `tests/ProjectCostForecast.UnitTests/NewMonthTests.cs`, `tests/ProjectCostForecast.UnitTests/ValidationBoundaryTests.cs`, `tests/ProjectCostForecast.UnitTests/ImportBoundaryTests.cs`, `tests/ProjectCostForecast.UnitTests/SafeCloseTests.cs`, `docs/ARCHITECTURE.md`, `docs/audit/POST-SOL-ARCHITECTURE-REMEDIATION-PLAN.md`, and this ledger.
Characterization evidence: the interleaving test pauses the first revision-aware writer immediately after its expected-revision check, starts a second service operation from the same original revision, and deterministically observes that both original tokens would pass the former check-only window. The shared writer boundary then permits the first replacement, makes the second re-read the new content hash, and returns `ProjectFileConflictException` with the winning actual revision. No timing sleep is used. Existing sequential stale-write, Reload / Save As / Cancel, backup/recovery, validation, migration, new-file, and atomic-write paths remain covered.
Implementation evidence: `ProjectFileWriteLock` uses a named Windows `Local` mutex derived from the canonical full destination path, shared by cooperating instances in one process or Windows interactive session. It does not retain a process-lifetime path dictionary. Abandoned ownership is recovered as a typed lease state because replacement is atomic; acquisition/release failures are typed `ProjectFileWriteLockException` errors. `IProjectFileService` no longer exposes unconditional `Save` or a default check-then-save implementation; bounded fakes implement `SaveWithRevision` explicitly. Non-cooperating writers and cross-host/network filesystem races remain outside the guarantee; this packet makes no universal filesystem-CAS claim.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore --filter "FullyQualifiedName~StaleWriteTests|FullyQualifiedName~Luna15FileWorkflowTests|FullyQualifiedName~BackupRecoveryTests|FullyQualifiedName~ValidationBoundaryTests"` exited 0 with 26 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, 214 discovered tests passed, and all 428 retained smoke assertions passed; `git diff --check` exited 0.
Residual risks / follow-up packet: this packet preserves whole-file conflict behavior rather than field-level merge, and it does not coordinate direct non-cooperating file writers or cross-host/network paths. MainWindow deferred-work lifetime closure remains LUNA-26C; the broader assembly split remains outside this corrective pass.

## LUNA-26C.1

Packet: LUNA-26C.1 - MainWindow lifecycle, edit, and data-mutation deferred-work closure
Status: complete
Baseline commit: `6806a71` (`docs(audit): split LUNA-26C lifecycle closure`)
Changed files: `src/ProjectCostForecast.App/MainWindow.ForecastGridInteraction.cs`, `src/ProjectCostForecast.App/MainWindow.GridBuilders.cs`, `src/ProjectCostForecast.App/MainWindow.Lifecycle.cs`, `src/ProjectCostForecast.App/MainWindow.ManagementResources.cs`, `src/ProjectCostForecast.App/MainWindow.ScheduleCommands.cs`, `src/ProjectCostForecast.App/MainWindow.SpreadsheetGridInteraction.cs`, `tests/ProjectCostForecast.UnitTests/Luna18ALifecycleTests.cs`, `tests/ProjectCostForecast.UnitTests/Luna26C1DeferredWorkTests.cs`, `docs/audit/POST-SOL-ARCHITECTURE-REMEDIATION-PLAN.md`, and this ledger.
Characterization evidence: `MainWindowWorkLifetime` is a pure generation-and-activity guard around an injected queue delegate. Its list-backed tests prove inactive and rejected queues return false, queued work is suppressed after unload/close or active data-context generation replacement, and a nested callback captures and honors its own generation. Source-order tests prove `MainWindow_DataContextChanged` increments the generation and clears pending state before rewiring or refresh. A comment/literal-aware source gate permits exactly one raw dispatcher schedule across the six-file packet scope, inside `QueueMainWindowWork`; qualified scheduling variants are covered by a negative control. No new test creates a WPF `Application`, `MainWindow`, STA host, or dispatcher loop.
Implementation evidence: `MainWindow.Lifecycle.cs` retains dispatcher shutdown-race handling and delegates all guarded execution through the lifetime seam. The five selected non-lifecycle partials preserve their original dispatcher priorities while routing deferred focus/edit, recalculation, category, schedule, and spreadsheet work through the owner. Deferred callbacks that previously captured a view model now reacquire the current data context, and rejected spreadsheet scheduling clears pending selection and edit-snapshot state. The pre-existing lifecycle expectation follows the extracted guard without weakening its assertion.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore --filter "FullyQualifiedName~Luna26C1DeferredWorkTests" /p:BaseOutputPath=artifacts/luna26c1-focused/` exited 0 with 12 focused tests passed; the same build with the LUNA-17/LUNA-18 and WPF compatibility filter exited 0 with 19 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, all 226 discovered tests passed, and all 428 retained smoke assertions passed; `git diff --check` exited 0.
Residual risks / follow-up packet: thirteen raw dispatcher scheduling sites remain intentionally reserved for LUNA-26C.2 across menu, filter, window-chrome, workspace-column, and workspace-panel partials. LUNA-26C.2 owns the all-MainWindow static gate and the final deterministic performance regression run; the broader assembly split remains outside this corrective pass.

## LUNA-26C.2

Packet: LUNA-26C.2 - MainWindow menu, workspace, shell, and final dispatcher architecture gate
Status: complete
Baseline commit: `5eaaf43` (`fix(audit): complete LUNA-26C.1 lifetime boundary`)
Changed files: `src/ProjectCostForecast.App/MainWindow.ColumnMenus.cs`, `src/ProjectCostForecast.App/MainWindow.GridFilters.cs`, `src/ProjectCostForecast.App/MainWindow.GridStatePills.cs`, `src/ProjectCostForecast.App/MainWindow.Lifecycle.cs`, `src/ProjectCostForecast.App/MainWindow.WindowChrome.cs`, `src/ProjectCostForecast.App/MainWindow.WorkspaceColumnState.cs`, `src/ProjectCostForecast.App/MainWindow.WorkspacePanels.cs`, `tests/ProjectCostForecast.UnitTests/Luna26C2DeferredWorkTests.cs`, `docs/audit/POST-SOL-ARCHITECTURE-REMEDIATION-PLAN.md`, and this ledger.
Characterization evidence: a comment/literal-aware source gate enumerates every source `MainWindow*.cs` partial and permits exactly one raw `BeginInvoke`/`InvokeAsync` call, inside `QueueMainWindowWork` in `MainWindow.Lifecycle.cs`; qualified scheduling variants are covered by a negative control. Source contracts enumerate all 13 converted sites and preserve Input, Normal, Loaded, Render, and Background priorities. They also prove menu-close ordering and instance ownership plus immediate rejection and generation-cancellation cleanup for KPI drag, ledger drag/zoom, and pending workspace editor state. The tests reuse the pure C.1 lifetime seam and create no WPF application or dispatcher host.
Implementation evidence: menu/filter focus, shell drag/zoom, current-row rendering, workspace editor focus, and detail-panel collapse now pass through the lifecycle owner. `ExecuteAfterClosingMenu` is instance-owned while `CloseContainingMenu` remains static. Rejected schedules clear transient flags immediately, and `CancelPendingWindowWork` clears them after accepted work is invalidated by unload, close, or data-context generation replacement. The production scope is seven files only because the pre-existing static forecast-group pill helper in `MainWindow.GridStatePills.cs` required one mechanical static-to-instance change to call the now instance-owned menu-closing helper; no other behavior changed there.
Commands and results: `dotnet test tests/ProjectCostForecast.UnitTests/ProjectCostForecast.UnitTests.csproj -c Release --no-restore --filter "FullyQualifiedName~Luna26C2DeferredWorkTests" /p:BaseOutputPath=artifacts/luna26c2-focused/` exited 0 with 6 focused tests passed; the same build with workspace, lifecycle, right-click/grid-pan, binding, and WPF filters exited 0 with 24 tests passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify.ps1' -NoRestore -RunLegacySmoke` exited 0 after a Release build with 0 warnings/errors, all 232 discovered tests passed, and all 428 retained smoke assertions passed; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\scripts\verify-performance.ps1' -OutputPath 'artifacts/luna26c2/performance-run.json' -BaselinePath 'docs/audit/LUNA-20-PERFORMANCE-BASELINE.json' -EnforceRegression -Iterations 3 -MemoryCycles 5` exited 0 with all 21 workload correctness checks passing and no median above baseline plus `max(50 ms, 25%)`; `git diff --check` exited 0.
Residual risks / follow-up packet: static source contracts characterize the MainWindow ownership boundary while representative existing WPF tests cover runtime surfaces; no synthetic process-global WPF host was added. SOL-01 must independently review and rerun the architecture, writer-conflict, binding, full-verification, and performance gates. The broader assembly split remains outside this corrective pass.
